package com.example.unihub.ui.Shared

import androidx.compose.foundation.layout.WindowInsets

/**
 * Zeroed window insets that can be used to opt-out from automatic system bar paddings
 * when the screen itself already applies the necessary safe area handling.
 */
val ZeroInsets = WindowInsets(0, 0, 0, 0)